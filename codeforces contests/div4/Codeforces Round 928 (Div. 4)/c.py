"""t = int(input())
for i in range(t):
    n = int(input())
    array = []
    for j in range(1,n+1):
        array.append(j)

    sums = []
    for num in array:
        total = 0
        for digit in str(num):
            total += int(digit)
        sums.append(total)


    print(sum(sums))"""

